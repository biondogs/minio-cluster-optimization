# MinIO Cluster Configuration
# Centralized configuration for MinIO cluster deployment

# Bootstrap credentials (change these for production!)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD='13qeadzc!#QEADZC'

# Cluster configuration
# All volumes across all nodes (note the http://host/path form)
MINIO_VOLUMES="http://minion{01...08}/mnt/e{1...2}d{1...5}e{1...12}"
MINIO_ERASURE_SET_DRIVE_COUNT=16
MINIO_STORAGE_CLASS_STANDARD=EC:6
MINIO_STORAGE_CLASS_RRS=EC:4

# Prometheus monitoring
MINIO_PROMETHEUS_URL=http://10.99.137.21:9091
MINIO_PROMETHEUS_JOB_ID=minio-job

# Server options
MINIO_OPTS="--console-address=:9001"

# Environment-specific overrides can be set in:
# /etc/default/minio (system-wide)
# Or as environment variables at runtime
